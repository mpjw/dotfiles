[user]
	email = marcus.wagner@tum.de
	name = Marcus Wagner
[credential]
	helper = cache
[merge]
	tool = vim -d
[alias]
		c = commit
		s = status
		st = status
		lg = log --oneline
		last = log -1 HEAD
		# cl = log -p -- ':(exclude)Cargo.lock'
		# f = push --force-with-lease
		# https://stackoverflow.com/a/11688523/472927
		# ss = "!f() { git stash show stash^{/$*} -p; }; f"
		# sa = "!f() { git stash apply stash^{/$*}; }; f"
		sl = stash list